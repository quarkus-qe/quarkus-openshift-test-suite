quarkus:
  application:
    name: test-http
  http:
    root-path: /api
  kubernetes:
    deployment-target: openshift
  openshift:
    expose: true
  s2i:
    base-jvm-image: registry.access.redhat.com/openjdk/openjdk-11-rhel7
